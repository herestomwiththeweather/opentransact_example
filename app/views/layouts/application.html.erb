<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>OpenTransact Example</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <%= link_to "OpenTransact Example", root_url, class: 'brand' %>
          <div class="container nav-collapse">
            <ul class="nav">
                <% if current_person %>
                  <li><%= link_to "New Asset", new_asset_path %></li>
                  <li><%= link_to "Edit Profile", edit_person_path(:current) %></li>
                  <li><%= link_to "Logout", logout_path %></li>
                <% else %>
                  <li><%= link_to "Register", signup_path %></li>
                  <li><%= link_to "Login", login_path %></li>
                <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="content">
          <div class="row">
            <div class="span9">
              <%= render :partial => 'shared/flashes' %>
            <%= yield %>
            </div>
            <div class="span1">&nbsp;</div>
            <div class="span3">
              <div class="well sidebar-nav">
              <% if current_person %>
                <h3>Session</h3>
                 <ul class="nav nav-list">
                     <li class="nav-header"><%= current_person.username %></li>
                     <li><p>Login at: <%= current_person.current_login_at.strftime("%I:%M %p on %b %d") %> from <%= current_person.current_login_ip %></p></li>
                     <% unless current_person.last_login_at.nil? %>
                       <li><p>Prev Login at: <%= current_person.last_login_at.strftime("%I:%M %p on %b %d") %> from <%= current_person.last_login_ip %></p></li>
                     <% end %>
                </ul>
              <% end %>
            </div>
           </div>
          </div>
        </div>

      <footer>
        <p>&copy; <%= link_to "Tom Brown", "https://github.com/herestomwiththeweather" %> 2012. Released under MIT license</p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
