<% if target.errors.any? %>
  <div id="error_explanation" class="alert alert-block alert-error validation-errors">
    <h4 class="alert-heading"><%= pluralize(target.errors.count, "error") %> prohibited this record from being saved:</h2>

    <ul>
    <% target.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
